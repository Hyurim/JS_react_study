import React from 'react';
import Button from '../../common/Button';

const QuestionList = () => {
    return (
        <div>
            질문 리스트
            <Button> 버튼 </Button>
        </div>
    );
};

export default QuestionList;